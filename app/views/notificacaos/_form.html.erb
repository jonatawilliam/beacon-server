<%= form_with(model: notificacao, local: true) do |form| %>
  <% if notificacao.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(notificacao.errors.count, "error") %> prohibited this notificacao from being saved:</h2>

      <ul>
      <% notificacao.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :titulo %>
    <%= form.text_field :titulo %>
  </div>

  <div class="field">
    <%= form.label :mensagem %>
    <%= form.text_area :mensagem %>
  </div>

  <div class="field">
    <%= form.label :beacon_id %>
    <%= form.text_field :beacon_id %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
